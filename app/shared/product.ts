export class Product {
    constructor(public category_id: number, public created_at: string, public description: string,
    public id: number, public name: string, public on_sale: string, public quantity: number,
    public updated_at: string, public user_id: number, public value: number
    ){}
}